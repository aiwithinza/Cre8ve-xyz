# Findings

## Skills
- `ui-ux-pro-max` is located at `skills/ui-ux-pro-max/`.
- `3D Website Asset Generator` is currently a subdirectory of `ui-ux-pro-max`.
- Both have `SKILL.md` files which is good.
- There are many other skills in the `skills/` directory.
- Local IP address is `192.168.1.3`. Dev server is now configured to bind to `0.0.0.0`.
